# 安全策略

## 支持的版本

XPTabBand 是一个开源的 Windows 资源管理器扩展，仅对最新发布版本提供安全修复。

| 版本 | 状态 | 说明 |
| ---- | ---- | ---- |
| 1.2.x | ✅ 支持 | 当前稳定版 |
| 1.1.x | ⚠️ 仅维护 | 严重安全问题修复 |
| < 1.1 | ❌ 不支持 | 请升级到最新版 |

## 报告漏洞

如果你发现安全漏洞，**请不要在公开 Issue 中提交**，以免被恶意利用。

请按以下方式私下报告：

1. 发送邮件到（待补充维护者邮箱）
2. 邮件主题以 `[SECURITY] XPTabBand` 开头
3. 描述：
   - 漏洞类型（DLL 劫持 / 权限提升 / 代码执行 / 信息泄露 等）
   - 影响范围和复现步骤
   - 受影响的版本
   - 可能的修复建议（可选）

## 响应时间

- **确认收到**：3 个工作日内
- **初步评估**：7 个工作日内
- **修复发布**：根据严重程度，30 天内发布补丁版本

严重等级参考 CVSS v3.1：

| 等级 | CVSS | 响应 |
| ---- | ---- | ---- |
| 严重 (Critical) | 9.0 - 10.0 | 7 天内发布补丁 |
| 高 (High) | 7.0 - 8.9 | 14 天内 |
| 中 (Medium) | 4.0 - 6.9 | 30 天内 |
| 低 (Low) | 0.1 - 3.9 | 下一个常规版本 |

## 安全相关的已知设计

为避免误报，以下是 XPTabBand **设计上**就会执行的操作，**不是**安全漏洞：

| 行为 | 原因 | 风险评估 |
| ---- | ---- | ---- |
| 安装时调用 `regsvr32` 注册 COM 组件 | Windows DeskBand 的标准注册方式，必须注册到 HKLM | 仅写入 CLSID 和 Toolbar 注册表项 |
| DLL 被 `explorer.exe` 加载到进程空间 | COM Apartment 模型要求，由系统调度 | 仅在当前用户会话中运行，不跨用户 |
| 收集日志到 `%LOCALAPPDATA%\Temp\XPTabBand_log.txt` | 用于调试，记录操作流程 | 仅本地路径，不上传任何数据 |
| 收藏夹数据写入 `%APPDATA%\XPTabCpp\favorites.dat` | 持久化用户偏好 | 仅包含 PIDL 和文件夹标题，无敏感信息 |

## 供应链安全

- 源码托管在 [GitHub](https://github.com/xp7777/XPTabBand)（主仓库）和 [Gitee](https://gitee.com/yxp1108/XPTabBand)（镜像）
- 发布的 `setup.exe` **未数字签名**，首次运行会被 SmartScreen 拦截
- 校验安装包完整性请以 GitHub Release 的 SHA-256 为准（发布时附带）
- **请只从官方 Release 页面下载**，第三方渠道的安装包可能被篡改

## 安全建议

1. **下载后校验哈希**：与 Release 页面公布的 SHA-256 对比
2. **不要将 DLL 复制到非安装目录**：可能被路径劫持利用
3. **卸载时一并清理**：卸载向导会询问是否保留收藏夹数据，敏感场景建议选"否"
4. **日志文件含路径信息**：分享日志前请脱敏路径中的用户名
5. **不要在生产环境启用 Debug 编译版本**：Debug 版本包含详细日志输出

## 致谢

感谢安全研究者负责任地披露漏洞。修复后将在 CHANGELOG 中致谢（除非报告者要求匿名）。
